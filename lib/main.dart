import 'package:flutter/material.dart';
import 'package:layouts_app/screen0-navega.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Screen0Navega(),
    );
  }
}
